node_modules/
build/*
!build/entitlements.mac.plist
!build/SIGNING.md
dist/
dist-electron/
release/
coverage/
test-results/
playwright-report/
.DS_Store
*.log
.env*
!.env.example

# CI runs `npm ci`, which requires a committed lockfile, and the third-party notices
# script reads it. npm is the single canonical package manager for this project.
sbom-cyclonedx.json

