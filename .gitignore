node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# This project locks dependencies with Bun (bun.lock). package-lock.json is only
# generated when installing via npm and must not be committed, or the two lockfiles
# will drift apart.
package-lock.json
